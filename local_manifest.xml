<?xml version="1.0" encoding="UTF-8"?>
<manifest>

  <!-- remote -->
  <remote name="aslenofarid"
          fetch="https://github.com/aslenofarid/"
          revision="lineage-17.1"/>

  <!-- device tree -->
  <project path="device/asus/X00TD"
           name="X00TD" 
           remote="aslenofarid"/>

  <!-- common tree -->
  <project path="device/asus/sdm660-common"
           name="common"
           remote="aslenofarid"/>

  <!-- vendor tree -->
  <project path="vendor/asus"
           name="vendor"
           remote="aslenofarid"/>

  <!-- kernel tree -->
  <project path="kernel/asus/sdm660"
           name="kernel_asus_sdm660"
           revision="lineage-17.1"
           remote="aslenofarid"/>

  <!-- remove project -->
  <remove-project name="LineageOS/android_hardware_qcom_audio"/>

  <!-- Hardware Qualcomm CAF Repo -->
  <project path="hardware/qcom-caf/msm8998/audio" name="hardware_qcom_audio" remote="aslenofarid"/>

</manifest>
